// import { Provider } from '@nestjs/common';
// import { Mongoose } from 'mongoose';
// import { MongooseAsyncProvider } from 'src/mongoose';
// import { PackageSchema } from './entities/package.entity';

// export const PackageModelProvider = 'PackageModelProvider';

// export const packageProvider: Provider = {
//   provide: PackageModelProvider,
//   inject: [MongooseAsyncProvider],
//   useFactory: (mongoose: Mongoose) => mongoose.model('Package', PackageSchema),
// };
